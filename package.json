{
  "name": "nyc-property-ecb",
  "version": "0.1.0",
  "private": true,
  "description": "NYC property resolver + ECB violations pipeline and API",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "api": "tsx src/api/main.ts",
    "scheduler": "tsx src/pipeline/scheduler.ts",
    "migrate": "tsx src/db/migrate-cli.ts",
    "seed": "tsx src/scripts/seed.ts",
    "ingest": "tsx src/scripts/ingest.ts",
    "import": "tsx src/scripts/import.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "fastify": "^5.3.0",
    "pg": "^8.13.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.7.0"
  }
}
